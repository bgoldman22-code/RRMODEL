[build]
  command = "npm run build"
  publish = "dist"

[functions]
  node_bundler = "esbuild"
  external_node_modules = ["@netlify/blobs","csv-parse"]

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
