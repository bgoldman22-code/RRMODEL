This patch adds:
- csv-parse to your package.json dependencies
- a [functions] block to netlify.toml with included_files for bundling _data/**

If your repo already has other fields in these files, copy the relevant snippets instead of fully replacing.