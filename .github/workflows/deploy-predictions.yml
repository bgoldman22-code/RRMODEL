# DISABLED - Replaced by nflverse-data-update.yml
name: Deploy NFL Predictions (DISABLED)
on:
  workflow_dispatch:
jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
    - run: echo 'DISABLED - Use nflverse-data-update.yml instead'; exit 1
