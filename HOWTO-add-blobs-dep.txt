Add @netlify/blobs dependency and commit lockfile:

1) Edit package.json (top level) to ensure:
{
  "type": "module",
  "dependencies": {
    "@netlify/blobs": "^6.0.1"
  }
}

2) Run locally once:
   npm install

This creates/updates package-lock.json. Commit both files.
