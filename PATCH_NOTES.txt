Patch: Switch ESM export to default for Netlify

File modified:
- netlify/functions/nfl-predictions-generate2/index.mjs

Change:
- Replace `export const handler = async (...) =>` with `export default async (...) =>`

Why:
- Your environment expects ESM default exports for functions.
- This resolves "handler not found" errors where Netlify fails to detect the handler.

Deploy steps:
1) Unzip into repo root (preserving path).
2) Commit and push.
3) Trigger "Clear cache and deploy" on Netlify.
4) Test:
   /.netlify/functions/nfl-predictions-generate2?season=2025&week=3

Changed: True
