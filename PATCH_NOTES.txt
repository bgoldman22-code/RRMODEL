Wire Weather/Travel: actual calls + adjusted confidence

- Updated netlify/functions/nfl-predictions-generate/index.mjs
  * Imports weather/travel modules
  * Inside prediction generation (.map over schedule):
      - Calls getWeatherImpact and travelImpact
      - Pushes weather/travel tags into factors
      - Computes adjustedConfidence (clamped 1..9)
      - Returns weather/travel fields on each row

Deploy:
1) Unzip into repo root
2) Commit & push
3) Netlify → Clear cache and deploy
4) Verify rows now include factors+weather+travel and confidence reflects adjustments
