Combined Patch: Build Fix + Weather/Travel Integration

Updates:
- netlify/functions/nfl-predictions-generate/index.mjs
    * Ensures imports:
        import { getWeatherImpact } from '../_lib/weather.mjs';
        import { travelImpact } from '../_lib/travel.mjs';
    * Converts sync map to:
        const rows = await Promise.all(schedule.map(async (game) => { ... })));
        const filteredRows = rows.filter(Boolean);
    * Injects Weather & Travel enrichment inside the map callback
        - calls getWeatherImpact(...) and travelImpact(...)
        - pushes tags into factors
        - computes adjustedConfidence (1..9 clamp)
    * Returns { rows: filteredRows, ... }
- netlify/functions/_lib/blobs.js
    * Adds missing exports:
        export const nflStore = getStore({...})
        export function diagBlobsEnv() { ... }

Deploy:
1) Unzip into repo root.
2) Commit & push.
3) Netlify → Clear cache and deploy.
