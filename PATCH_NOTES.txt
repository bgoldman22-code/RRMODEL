Wire Weather/Travel Into Predictions

Updated:
- netlify/functions/nfl-predictions-generate/index.mjs

Changes:
- Import getWeatherImpact/travelImpact if missing
- Enrich each row with:
  * row.weather, row.travel
  * factors += weather/travel tags
  * confidence adjustments (supports moneylineConf 0..1 or confidence 1..9)

Resilience:
- Catches weather fetch errors/rate limits (returns null)
- No crash if API key is missing; weatherData becomes null

Deploy:
1) Unzip into repo root.
2) Commit & push.
3) Netlify → Clear cache and deploy.
4) Verify response rows include .weather/.travel/.factors and confidence updated.