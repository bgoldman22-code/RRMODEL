{
  "name": "rrmodel",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node -e \"require('fs').mkdirSync('dist',{recursive:true}); require('fs').writeFileSync('dist/index.html','OK'); console.log('dist prepared')\""
  },
  "dependencies": {
    "@netlify/blobs": "^6.2.0",
    "csv-parse": "^5.5.6"
  }
}