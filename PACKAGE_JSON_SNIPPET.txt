// Ensure your package.json has at least:
{
  "scripts": {
    "prebuild": "node ./scripts/prebuild-check.mjs",
    "build": "vite build",
    "dev": "vite",
    "preview": "vite preview --port 4173"
  }
}
